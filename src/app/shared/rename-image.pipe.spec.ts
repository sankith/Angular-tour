import { RenameImagePipe } from './rename-image.pipe';

describe('RenameImagePipe', () => {
  it('create an instance', () => {
    const pipe = new RenameImagePipe();
    expect(pipe).toBeTruthy();
  });
});
